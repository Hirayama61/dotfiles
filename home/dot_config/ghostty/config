# Ghostty configuration
# Reference: https://ghostty.org/docs/config
#
# 方針: tmux 中心の運用なのでターミナル単体機能は最小限。
# タブ関連はデフォルトのまま使う。

# テーマ (themes/ 配下のカスタムテーマを参照)
theme = panda

# フォント (Brewfile の font-jetbrains-mono-nerd-font。Nerd Font アイコン込み)
# 反映されない場合は `ghostty +list-fonts | grep -i jetbrains` でファミリ名を確認する。
# Mono = アイコンも等幅でターミナル向き。
font-family = JetBrainsMono Nerd Font Mono
# 日本語フォールバック (JetBrains Mono は和文グリフ非搭載。未指定だと Ghostty が
# Osaka/BIZ UDGothic 等へ勝手に落ちて字形・字幅が乱れる)。2 行目以降の font-family
# は欠落コードポイント用フォールバック。試行中: Hiragino → 不満なら合成フォントへ。
font-family = "Hiragino Sans"
font-size = 14

# ウィンドウ
window-padding-x = 12
window-padding-y = 8
window-padding-balance = true
macos-titlebar-style = hidden

# マウス
copy-on-select = clipboard
mouse-hide-while-typing = true

# クリップボード
clipboard-write = allow
clipboard-trim-trailing-spaces = true

# シェル統合
shell-integration = zsh
